function setup() {
  createCanvas(1000, 1000);
  background(220, 20, 60);
  
  var p = 1000 ;
  
  fill(34, 139, 34);
  stroke(34, 139, 34);
  ellipse(p/2, p/2, 500, 500);
  
  fill(0, 0, 0);
  stroke(0, 0, 0);
  rect(p/2, 0, 100, p);
  rect(0, p/2, p, 100  )
}

function draw() {
  
}
